常用模块一:
    collection
    time
    random
    os
    sys
    序列化
    re

正则表达式:
    # 从目标文件中找到符合规则的内容
        a.爬虫:解析网页代码,获取内容
        b.日志分析:
        c.表单验证
re
    字符组:[] 写在中括号中的内容,都能匹配
        [0-9] 匹配数字
        [a-z] 匹配小写字母
        [A-Z] 匹配大写字母
        [a-zA-Z] 匹配大小写字母


常用模块二:
    hashlib
    configparse
    logging


import os   # 文件操作
os.remove()     # 删除文件
os.rename()     # 重命名文件7